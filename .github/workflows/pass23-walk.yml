name: pass23-walk
on: workflow_dispatch
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: name the cluster the read holds
        run: echo aks-harnessmatters-eus2
        env:
          resource_group_name: rg-harnessmatters
